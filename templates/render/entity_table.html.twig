{% extends 'component/view/base.html.twig' %}

{% block css %}
{% endblock %}

{% block javascripts %}
{% endblock %}

{% block body %}
    <div class="row">
        <h5 class="col title">
            {{ title }}
        </h5>
    </div>
    <div class="row">
        <div class="col">
            <table class="table table-hover" id="user-table"
                   data-entity-table
                   data-btn="{{ btn }}"
                   data-column="{{ column }}"
            >
                <thead class="thead-light">
                <tr>
                    {% for h in header %}
                        <th>{{ h }}</th>
                    {% endfor %}
                </tr>
                </thead>
                <tbody>
                {% for index, row in table %}
                    <tr id="{{ index }}" data-param="{{ row["param"] }}">
                        {% for col in row["content"] %}
                            <td>{{ col }}</td>
                        {% endfor %}
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}